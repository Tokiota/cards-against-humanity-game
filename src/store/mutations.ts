import * as types from "./state-types";
import { State } from "./state";


export const mutations = {
    // [types.GET_PRODUCT] (state: State, product: Product) {
    //     state.product = product;
    // },

}
