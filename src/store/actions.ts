import { ActionContext } from 'vuex/types';
import { State } from './state'
import * as types from './state-types'



export const actions = {
    // [types.GET_PRODUCT](actionContext: ActionContext<State, State>): Promise<Product[]> {
    //     var api = new ProductDataService();
    //     var promise = api.getProduct(actionContext.state.productId, actionContext.state.language);
    //     promise.then(data => {
    //         actionContext.commit(types.GET_PRODUCT, data);
    //     });
    //     return promise;
    // },
};
